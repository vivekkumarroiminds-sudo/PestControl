Drop-in image assets for the site. All are optional — the pages render with
inline SVG/placeholder fallbacks until you add the real files here.

- hero.jpg      — homepage hero background (auto-replaces the SVG scene). ~1600×900.
- team.jpg      — "About the company" photo (auto-replaces the SVG). ~600×460.
- og-cover.jpg  — social/Google share preview image. Exactly 1200×630 recommended.
                  Referenced by the Open Graph / Twitter meta tags on every page.
- favicon.svg   — browser tab icon (already included; replace to rebrand).
